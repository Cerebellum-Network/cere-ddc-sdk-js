version: '3.4'

services:
  cere-chain:
    platform: linux/amd64
    container_name: cere-chain
    # TODO: Switch back to dev-latest when possible
    image: '625402836641.dkr.ecr.us-west-2.amazonaws.com/pos-network-node:deployment-0.9.30-latest'
    command: bash -c "/usr/local/bin/cere --dev --alice --ws-external --rpc-external"
    ports:
      - '9944:9944'
      - '9933:9933'
