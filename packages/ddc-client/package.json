{
  "name": "@cere-ddc-sdk/ddc-client",
  "description": "DDC client",
  "version": "1.7.5-beta.2",
  "type": "module",
  "repository": {
    "type": "git",
    "directory": "packages/ddc-client",
    "url": "git+https://github.com/Cerebellum-Network/cere-ddc-sdk-js.git"
  },
  "exports": {
    ".": {
      "browser": "./browser.js",
      "require": "./index.cjs",
      "import": "./index.js",
      "default": "./index.js"
    },
    "./browser": {
      "require": "./browser.cjs",
      "import": "./browser.js",
      "default": "./browser.js"
    }
  },
  "types": "index.d.ts",
  "typesVersions": {
    "*": {
      "browser": [
        "browser"
      ]
    }
  },
  "author": "",
  "license": "Apache-2.0",
  "scripts": {
    "build:cjs": "TARGET=build:cjs NODE_ENV=production webpack",
    "build:mjs": "TARGET=build:mjs NODE_ENV=production webpack",
    "build:browser": "BROWSERSLIST_ENV=browser npm run build:cjs && BROWSERSLIST_ENV=browser npm run build:mjs",
    "build:browser:module": "TARGET=build:mjs BROWSERSLIST_ENV=browser NODE_ENV=production webpack",
    "build:node": "BROWSERSLIST_ENV=node npm run build:cjs && BROWSERSLIST_ENV=node npm run build:mjs",
    "build": "npm run build:browser && npm run build:node",
    "push": "node scripts/publish.js",
    "types": "tsc -p tsconfig.build.json --emitDeclarationOnly",
    "package": "rimraf build && clean-publish --temp-dir build && npm run types && npm run build"
  },
  "dependencies": {
    "@cere-ddc-sdk/content-addressable-storage": "1.7.5-beta.2",
    "@cere-ddc-sdk/core": "1.7.5-beta.2",
    "@cere-ddc-sdk/file-storage": "1.7.5-beta.2",
    "@cere-ddc-sdk/key-value-storage": "1.7.5-beta.2",
    "@cere-ddc-sdk/smart-contract": "1.7.5-beta.2",
    "@polkadot/util": "~9.7.2",
    "@polkadot/util-crypto": "~9.7.2"
  },
  "devDependencies": {
    "@polkadot/extension-inject": "0.44.6",
    "@types/node": "^16.0.0",
    "buffer": "^6.0.3",
    "clean-publish": "^4.0.1",
    "rimraf": "^3.0.2",
    "typescript": "4.5.4",
    "tweetnacl": "^1.0.3"
  }
}
