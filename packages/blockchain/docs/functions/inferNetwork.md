[**@cere-ddc-sdk/blockchain**](../README.md)

***

[@cere-ddc-sdk/blockchain](../README.md) / inferNetwork

# Function: inferNetwork()

> **inferNetwork**(`wsUrl`): `CereNetwork`

Infer the network from a bare WS URL — back-compat for `connect(url)`.

## Parameters

### wsUrl

`string`

## Returns

`CereNetwork`
